
const Mark = ({ markTitle }) => {
    // console.log(markTitle);

    return (
        <div>
            <h1 className=" p-2 bg-white rounded-md">{markTitle}</h1>
        </div>
    );
};
export default Mark;